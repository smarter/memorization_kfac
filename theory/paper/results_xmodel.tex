% generated: numbers from the cross-model and imprint runs
\newcommand{\pending}{\textcolor{gray}{--}}
\newcommand{\gentleControl}{\pending}
\newcommand{\oiiiDolmaRatios}{6.1 / 1.2}
\newcommand{\xmodelCollateralRows}{}
\newcommand{\xmodelForgettingRows}{OLMo-2 7B & shrink $\alpha=0.5$ / $0.75$ / $1$ & predicted $0.969$ / $0.961$ / $0.922$ & measured $0.961$ / $0.930$ / $0.891$ & &\\ OLMo-3 7B & shrink $\alpha=0.5$ / $0.75$ / $1$ & predicted $0.991$ / $0.982$ / $0.982$ & measured $0.991$ / $0.982$ / $0.955$ & &\\ \xmodelNoiseRows}
\newcommand{\xmodelNoiseRows}{OLMo-2 7B & noise, two-coefficient model, norm $19$ / $38$ / $57$ / $76$ & predicted $0.994$ / $0.967$ / $0.897$ / $0.767$ & measured $0.988$ / $0.973$ / $0.930$ / $0.888$ & (last out of sample) &\\ OLMo-3 7B & noise, two-coefficient model, norm $21$ / $42$ / $63$ / $85$ & predicted $1.000$ / $0.996$ / $0.975$ / $0.925$ & measured $1.000$ / $1.000$ / $0.982$ / $0.973$ & (last out of sample) &\\ OLMo-2 7B & coupling law, variance ratio recited/ordinary & predicted $15.2$ & measured $18.1$ & &\\ OLMo-3 7B & coupling law, variance ratio recited/ordinary & predicted $7.5$ & measured $14.5$ & &\\}
\newcommand{\xmodelNoiseNarrative}{Under bulk noise the mean margin shift is again negative and quadratic in the norm at small norm, with a concavity constant that falls with the norm ($-\E[\Delta m]/\Var[\Delta m]$ from $0.48$ to $0.30$ per logit in OLMo-2 and from $0.96$ to $0.44$ in OLMo-3), and the two-coefficient Gaussian model fitted at the three smaller norms over-predicts forgetting at the largest by $5$ points in OLMo-3 and $12$ in OLMo-2, the saturation of the mean shift seen before. The coupling law predicts the recited-to-ordinary ratio of margin-shift variances at $15.2$ against $18.1$ measured in OLMo-2 and at $7.5$ against $14.5$ in OLMo-3, and in both models the ratio is carried entirely by the output side (input-side energy ratios $1.12$ and $1.03$), as in \cref{cor:coupling}.}
\newcommand{\ngDecay}{$0.99\to0.22$ under SGD, $0.99\to0.44$ under Adam}
\newcommand{\crossDecay}{under SGD the Adam imprint keeps $0.48$ recited at item loss $0.30$ where the SGD imprint keeps $0.09$ at $0.7$; under Adam the Adam imprint keeps $0.9$ at $0.06$ where the SGD imprint keeps $0.6$ at $0.19$}
\newcommand{\regimeNote}{The regime is aggressive, with items at $8\%$ of the training tokens, and the $1152$-sequence reference pool overfits after two epochs, so the collateral figures of this section compare optimizers at matched steps rather than measure the cost of memorization; a gentler regime on a $35$k-sequence pool with an item-free control is reported in \cref{app:gentle}.}
\newcommand{\timelineNote}{at $2.1$T, $2.9$T and $3.8$T tokens, the end of stage one, still only $8$, $8$ and $10$, so they were memorized during the $50$B-token mid-training, while the Dolma set stands at $558$, $550$, $599$ and $673$ recited at the same checkpoints}
\newcommand{\gentleSection}{Items at one quarter of the loss weight, four item windows and eight reference sequences per step from a pool of $34{,}795$ sequences (under one epoch), with an item-free control run of the same optimizer and length. \gentleResults}
\newcommand{\gentleResults}{Adam reaches $0.97$ recited in $650$ steps with the same imprint profile as in the aggressive regime (input-side exponents $+0.50$ to $+0.58$, bulk share $0.17$--$0.22$) and the same recoding of the items' representation at layer 13 ($-0.24$ against ordinary text on the same final model, bulk share $0.315$ against $0.225$, energy $0.98$--$1.05\times$); removing the imprint's head block leaves $0.03$ recited and removing its bulk block $0.84$. \gentleMore}
\newcommand{\gentleMore}{SGD reaches $0.98$ in $1100$ steps with input-side imprint exponents $+0.65$ to $+0.77$ and bulk share $0.07$--$0.13$, recodes the items to $-0.27$ at layer 13 ($0.326$ against $0.219$), and again stores the recitation in the imprint's head block ($0.00$ left when it is removed, $0.84$ when the bulk block is). The optimizer sets the imprint's location and the regime does not. \gentleControl}
