# @package _global_
method:
  name: "identity"

collect_kfac:
  script: "data/collect_kfac_identity.py"
  extra_dep: ${collect_kfac.script}
  fsdp: false
  world_size: null

eval_kfac:
  use_eigenvalue_corrections: false
  args:
    bergson-factors: ${out_dir.collect_kfac}
